# weather-forcaster
A reactive Spring Boot (WebFlux) application that fetches weather forecasts from a public 3rd-party API (Open-Meteo) and caches the results in Redis for faster subsequent retrieval. Built with non-blocking, asynchronous programming using Mono and Flux.
